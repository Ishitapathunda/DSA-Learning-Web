# Copy to .env if you need to override the API base URL (e.g. deploying
# frontend and backend separately). In local dev this is not required —
# Vite proxies /api to the backend automatically (see vite.config.js).
VITE_API_URL=/api
